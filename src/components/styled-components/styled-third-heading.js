import styled from "styled-components";
import * as M from "./mixins";

const StyledThirdHeading = styled.p`
  ${M.marginLeftRight}
  font-size: 3rem;
  margin-bottom: 3rem;
`;

export default StyledThirdHeading;
