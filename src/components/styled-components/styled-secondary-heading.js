import styled from "styled-components";
import * as M from "./mixins";

const StyledSecondaryHeading = styled.h3`
  font-size: 4.5rem;
  ${M.paddingsLeftRight}
  margin-bottom: 5rem;
`;

export default StyledSecondaryHeading;
