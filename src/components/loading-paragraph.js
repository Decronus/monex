import StyledLoadingParagraph from "./styled-components/styled-loading-paragraph";

const LoadingParagraph = ({ text }) => {
  return <StyledLoadingParagraph>{text}</StyledLoadingParagraph>;
};

export default LoadingParagraph;
