import StyledThirdHeading from "./styled-components/styled-third-heading";

const ThirdHeading = ({ heading }) => {
  return <StyledThirdHeading>{heading}</StyledThirdHeading>;
};

export default ThirdHeading;
